---
permalink: ./pages.json
permalinkBypassOutputDir: true
eleventyExcludeFromCollections: true
---
[{% for post in collections.posts %}
	{
   "title":"{% if post.data.title %}{{ post.data.title | addNbsp | safe }}{% else %}{{ meta.siteName }}{% endif %}",
   "imgName":"{% if post.data.title %}{{ post.data.title | slug}}{% else %}home{% endif %}"
	}{% if loop.last == false %},{% endif %}
{% endfor %}]